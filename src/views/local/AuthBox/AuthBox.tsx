import React from 'react'

import { Box, BoxProps } from '@mui/material'

interface IAuthBoxProps {
  
}

const AuthBox = (props : IAuthBoxProps)  => {
  return <Box>
    
  </Box>
}

export default AuthBox