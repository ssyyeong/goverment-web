import React from 'react';

import { Box, BoxProps } from '@mui/material';

interface ISnsLoginProps {}

const SnsLogin = (props: ISnsLoginProps) => {
	return <Box></Box>;
};

export default SnsLogin;
