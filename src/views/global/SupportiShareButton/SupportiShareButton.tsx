import React from 'react';

import { Box, BoxProps } from '@mui/material';

interface ISupportiShareButtonProps {}

const SupportiShareButton = (props: ISupportiShareButtonProps) => {
	return <Box></Box>;
};

export default SupportiShareButton;
